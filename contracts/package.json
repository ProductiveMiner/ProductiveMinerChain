{
  "name": "productiveminer-contracts-minimal",
  "version": "1.0.0",
  "description": "Minimal contract deployment for ProductiveMiner Asymptotic Token System",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-asymptotic-token-system.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.4",
    "@openzeppelin/contracts": "^5.4.0",
    "ethers": "^6.8.1",
    "hardhat": "^2.19.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
